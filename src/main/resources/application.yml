spring:
  config:
    use-legacy-processing: true
#    activate:
#      on-profile: local

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/restdocs?characterEncoding=UTF-8&serverTimezone=Asia/Seoul&allowMultiQueries=true&zeroDateTimeBehavior=convertToNull
    username: root
    password: petfins0905!

    hikari:
      pool-name: hikari-cp
      maximum-pool-size: 30
      minimum-idle: 2

    jpa:
      properties:
        hibernate:
          format_sql: true
      show-sql: true

  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

    main:
      allow-circular-references: true

server:
  port: 80